package com.Scaler.BookMy.Show.controller.utils;

import com.Scaler.BookMy.Show.dto.UserRequestdto;

public class UserControllerUtils {
    public static void validate(UserRequestdto userRequestdto){
        //if (userRequestdto.getEmail().equals())
        //write logic for comparing email to all available email.
        //check can we create hashset of emails in service layer and add all emails ,on creation of user.
        //this hashset can be used for validating the presense of email.
    }
}
